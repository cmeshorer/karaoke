/**
 * Determines if a number is even
 * @param  {number} number The number
 * @return {boolean} Whether even is true
 */
export const isEvenNumber = (number: number) => {
  return number % 2 === 0;
};
